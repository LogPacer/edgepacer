# Solutions

This directory contains verified, reusable implementation knowledge from shipped changes.

- [`architecture-patterns/`](architecture-patterns/) — contracts and design patterns that should survive later refactors.

Each solution records its source pull request and the default-branch revision used to verify the claims. Prefer updating an overlapping solution over adding a second document for the same invariant.
