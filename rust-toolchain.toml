# Pin the build toolchain so cross/cargo don't silently fall back to the
# machine's rustup default (which lagged at 1.91 while sdjournal needs 1.95).
# build-all.sh unsets RUSTUP_TOOLCHAIN, so this file is what wins there.
[toolchain]
channel = "1.95.0"
